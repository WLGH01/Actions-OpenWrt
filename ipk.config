# Independent IPK build configuration.
# Add one CONFIG_PACKAGE_<package>=m line per package to compile.
# The workflow builds these packages only; it does not build a firmware image.
CONFIG_PACKAGE_luci-app-daed=m
CONFIG_PACKAGE_daed=m
CONFIG_PACKAGE_daed-geoip=m
CONFIG_PACKAGE_daed-geosite=m
